import { getDb } from "../../../db";import { games } from "../../../db/schema";import { and,eq } from "drizzle-orm";import{notFound}from"next/navigation";
export const dynamic="force-dynamic";export default async function Game({params}:{params:Promise<{slug:string}>}){const{slug}=await params;let g:any;try{g=(await getDb().select().from(games).where(and(eq(games.slug,slug),eq(games.published,true))).limit(1))[0]}catch{}if(!g)return notFound();return <main style={{maxWidth:900,margin:"auto",padding:48}}><nav>Home / Games / {g.name}</nav><h1>{g.name}</h1><dl><dt>Status</dt><dd>{g.status}</dd><dt>Platforms</dt><dd>{g.platforms}</dd><dt>Business model</dt><dd>{g.businessModel}</dd><dt>Combat</dt><dd>{g.combat}</dd><dt>Setting</dt><dd>{g.setting}</dd><dt>Focus</dt><dd>{g.focus}</dd><dt>Release</dt><dd>{g.releaseDate||"Not confirmed"}</dd></dl>{g.officialUrl&&<p><a href={g.officialUrl}>Official website</a></p>}<p>Source: <a href={g.sourceUrl}>{g.sourceUrl}</a> · Fact-checked: {g.factCheckedAt}</p><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"VideoGame",name:g.name,gamePlatform:g.platforms,datePublished:g.releaseDate||undefined})}}/></main>}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;return{title:`${slug.replaceAll("-"," ")} | MyRPG.IO`,alternates:{canonical:`https://myrpg.io/games/${slug}`}}}
