import { getDb } from "../../../db"; import { games } from "../../../db/schema"; import { and,eq } from "drizzle-orm"; import { notFound } from "next/navigation"; import { PublicPage } from "../../components/PublicChrome";
export const dynamic="force-dynamic";
export default async function Game({params}:{params:Promise<{slug:string}>}){const{slug}=await params;let g:any;try{g=(await getDb().select().from(games).where(and(eq(games.slug,slug),eq(games.published,true))).limit(1))[0]}catch{}if(!g)return notFound();return <PublicPage><nav aria-label="Breadcrumb"><a href="/">Home</a> / <a href="/games">Games</a> / {g.name}</nav><p style={eyebrow}>HUMAN-APPROVED GAME PROFILE</p><h1 style={heading}>{g.name}</h1><p style={muted}>Recently fact-checked: {g.factCheckedAt?.slice(0,10)}</p><dl style={facts}>{[["Status",g.status],["Platforms",g.platforms],["Business model",g.businessModel],["Combat",g.combat],["Setting",g.setting],["Focus",g.focus],["Release",g.releaseDate||"Not confirmed"]].map(([label,value])=><div key={label as string}><dt>{label}</dt><dd>{value}</dd></div>)}</dl>{g.officialUrl&&<p><a href={g.officialUrl} target="_blank" rel="noopener noreferrer" style={link}>Official website →</a></p>}<section style={source}><h2>Source & editorial notes</h2><p>This profile uses visible, owner-approved factual fields only. <a href={g.sourceUrl} target="_blank" rel="noopener noreferrer" style={link}>Official source</a></p></section><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"VideoGame",name:g.name,gamePlatform:g.platforms,datePublished:g.releaseDate||undefined})}}/></PublicPage>}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;return{title:`${slug.replaceAll("-"," ")} | MyRPG.IO`,alternates:{canonical:`https://myrpg.io/games/${slug}`}}}
const eyebrow={color:"#76f5e3",fontSize:11,fontWeight:800,letterSpacing:1.5,marginTop:30};const heading={fontSize:"clamp(2.6rem,7vw,5rem)",letterSpacing:"-.06em",margin:"12px 0"};const muted={color:"#aeb6c7"};const facts={display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(210px,1fr))",gap:12,borderTop:"1px solid #273044",marginTop:30,paddingTop:20};const link={color:"#76f5e3"};const source={borderLeft:"3px solid #76f5e3",padding:"16px 20px",marginTop:32,background:"#101521",color:"#aeb6c7"};
