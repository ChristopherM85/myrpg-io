type Props = { title: string; category?: string; label?: string; eager?: boolean; image?: { assetUrl: string | null; sourceUrl?: string | null; altText: string; caption: string | null; credit: string | null; width: number | null; height: number | null } };

export function EditorialVisual({ title, category = "MMO intelligence", label = "Editorial brief", eager = true, image }: Props) {
  const src = image?.assetUrl || image?.sourceUrl;
  if (src) return <figure style={{ margin: "32px 0", border: "1px solid #27344e", background: "#101623", boxShadow: "0 18px 46px rgba(0,0,0,.24)" }}><div style={{ position: "relative", overflow: "hidden" }}><img src={src} alt={image.altText} width={image.width ?? 1200} height={image.height ?? 675} style={{ display: "block", width: "100%", height: "auto", aspectRatio: "16 / 9", objectFit: "cover" }} loading={eager ? "eager" : "lazy"} /><div aria-hidden="true" style={{ position: "absolute", inset: 0, background: "linear-gradient(180deg,transparent 52%,rgba(5,8,14,.78))" }} /></div><figcaption style={{ padding: "11px 14px", fontSize: 12, color: "#aeb9c7", display: "flex", gap: 10, justifyContent: "space-between", flexWrap: "wrap" }}><span>{image.caption || image.altText}</span><span>{image.credit || "Approved editorial media"}</span></figcaption></figure>;
  return <figure aria-label="MyRPG editorial graphic — not gameplay" style={{ margin: "32px 0", minHeight: 310, padding: 30, display: "flex", flexDirection: "column", justifyContent: "end", background: "radial-gradient(circle at 80% 10%,#194f72 0,transparent 36%),repeating-linear-gradient(90deg,transparent 0 35px,rgba(118,245,227,.045) 36px),linear-gradient(135deg,#11182b,#10101b 55%,#16263c)", border: "1px solid #31547a", boxShadow: "0 18px 46px rgba(0,0,0,.24)", color: "#edf7fb" }}><small style={{ letterSpacing: 2, color: "#77e7ec", fontWeight: 800 }}>MYRPG EDITORIAL GRAPHIC — NOT GAMEPLAY</small><h2 style={{ fontSize: "clamp(1.7rem,4vw,3rem)", maxWidth: 700, margin: "14px 0 8px" }}>{title}</h2><p style={{ margin: 0, color: "#b7c9d6" }}>{category} · {label}</p></figure>;
}
