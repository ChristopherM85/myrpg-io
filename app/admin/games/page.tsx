import {requireChatGPTUser}from"../../chatgpt-auth";import{getDb}from"../../../db";import{games,users}from"../../../db/schema";import{eq}from"drizzle-orm";import GameManager from"./manager";export const dynamic="force-dynamic";export default async function AdminGames(){const user=await requireChatGPTUser("/admin/games");let rows:any[]=[];let role="editor";try{const db=getDb();role=(await db.select().from(users).where(eq(users.email,user.email)).limit(1))[0]?.role||role;rows=await db.select().from(games)}catch{}return <GameManager role={role} games={rows}/>}
