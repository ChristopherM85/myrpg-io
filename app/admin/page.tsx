import { requireChatGPTUser } from "../chatgpt-auth";
export const dynamic = "force-dynamic";
export default async function AdminPage() { const user = await requireChatGPTUser("/admin"); return <main style={{padding:"48px",fontFamily:"Arial",background:"#090b12",color:"#f0f0ed",minHeight:"100vh"}}><p style={{color:"#76f5e3",letterSpacing:2}}>MYRPG / DIRECTOR CONSOLE</p><h1>Protected operations workspace</h1><p>Signed in as {user.email}. Live agent execution is disabled. Owner setup, D1 records, and approval-only publishing are required before any content can reach players.</p><section><h2>Pipeline</h2><p>Scout → Research → Validator → Editor → Human approval → Published / Rejected</p><h2>Safety defaults</h2><ul><li>20 scout candidates per run</li><li>5 research jobs per run</li><li>3 editorial drafts per run</li><li>Manual publishing only</li><li>Daily and per-job caps enforced by policy</li></ul></section></main>; }
