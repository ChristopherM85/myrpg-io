import{getDb}from"../../db";import{articles}from"../../db/schema";import{eq}from"drizzle-orm";export const dynamic="force-dynamic";export const metadata={title:"MMO News | MyRPG.IO",description:"Human-reviewed MMO news with source-linked editorial notes."};export default async function News(){let rows:any[]=[];try{rows=await getDb().select().from(articles).where(eq(articles.status,"published"))}catch{}return <main style={{maxWidth:900,margin:"auto",padding:48}}><p>MYRPG / NEWS</p><h1>MMO News</h1><p>Source-linked and human-reviewed. No autonomous publishing.</p>{rows.length?rows.map(a=><article key={a.id}><h2><a href={`/articles/${a.slug}`}>{a.title}</a></h2><p>{a.summary}</p><small>Fact-checked: {a.factCheckedAt} · <a href={a.sourceUrl}>Source</a> · AI-assisted, human-reviewed</small></article>):<p>Published coverage will appear after human review.</p>}</main>}
